__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.local

# State is now committed back to the repo by the workflow for a durable
# audit trail — dedup state, alert ledger, and scoring outcomes all live
# under state/ in git history. Nothing under state/ should be ignored.
